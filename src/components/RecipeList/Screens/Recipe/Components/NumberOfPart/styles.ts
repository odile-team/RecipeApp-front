// @ts-ignore
import styled from '@emotion/native';
import { colors } from 'globalStyles/color';

export const NumberOfPartContainer = styled.View`
  flex-direction: row;
`;
export const NumberOfPartText = styled.Text`
  padding-left: 5px;
  color: ${colors.grey};
  font-size: 13px;
`;
