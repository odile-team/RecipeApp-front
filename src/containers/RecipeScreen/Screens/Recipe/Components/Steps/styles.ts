// @ts-ignore
import styled from '@emotion/native';
import { colors } from 'globalStyles/color';

export const StepsContainer = styled.View`
  background: ${colors.mainBlue}20;
  padding: 15px;
  margin-top: 10px;
  border-radius: 20px;
`;

export const StepTitle = styled.Text`
  color: ${colors.mainBlue};
  font-weight: bold;
  font-size: 15px;
  padding-bottom: 10px;
`;
export const StepDesc = styled.Text``;
