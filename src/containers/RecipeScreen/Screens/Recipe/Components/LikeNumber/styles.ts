// @ts-ignore
import styled from '@emotion/native';
import { colors } from 'globalStyles/color';

export const LikeNumberContainer = styled.View`
  justify-content: center;
  align-items: center;
  flex-direction: row;
`;

export const LikeNumberText = styled.Text`
  padding-left: 7px;
  color: ${colors.grey};
  font-size: 13px;
`;
